<UserControl x:Class="TimeKeep.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:inf="clr-namespace:Infrastructure;assembly=Infrastructure"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel Background="White">
        <Label DockPanel.Dock="Top" Content="{Binding Name}"  FontSize="20"  HorizontalAlignment="Left" />
        
        <!--LocalContentRegion-->
        <!--<ContentControl prism:RegionManager.RegionName="{x:Static inf:RegionNames.LocalContentRegion}" />-->
        <ListView ></ListView>
    </DockPanel>
</UserControl>
